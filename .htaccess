# Prevent viewing of .htaccess file
<Files .htaccess>
order allow,deny
deny from all
</Files>

# Prevent directory listings
Options All -Indexes

#Protect Site
AuthName "EasyArbs Members Login"
AuthType Basic
AuthUserFile .htpasswd
Require valid-user
